# Loaded automatically by `next build` (production) — overrides .env.
# NEXT_PUBLIC_* values are inlined into the bundle at BUILD time, so the
# canonical site URL must be set here, not only in wrangler.jsonc vars.
# Contains no secrets — safe to commit.
NEXT_PUBLIC_APP_URL=https://www.grainroots.com.np
